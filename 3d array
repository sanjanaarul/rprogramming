# Create data values (24 elements = 4 cols * 3 rows * 2 tables)
values <- 1:24

# Create array: 3 rows, 4 columns, 2 tables
my_array <- array(values, dim = c(3, 4, 2))

# Display array
print(my_array)
